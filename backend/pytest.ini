[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
log_cli = false
log_level = WARNING

# Ensure conftest env vars are seen before collection
# (conftest.py sets os.environ before imports)
