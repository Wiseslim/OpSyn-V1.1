 OPSYN PLATFORM Enterprise Workflow Engine Corrective Implementation Plan — Gap Analysis Report Document Type Corrective Implementation Plan Phase Phase 5 — Enterprise Workflow Engine (Post-Implementation Review) Date May 16, 2026 Source Commit 5334893 (main) Prepared By Platform Architecture Review — SlimTech Scope Cross-reference: Original Prompt vs Implementation Plan vs Implementation Report 1. Executive Summary This document is the result of a three-way cross-reference between (1) the original full implementation prompt, (2) the Opsyn Phase 5 Enterprise Workflow Engine Plan (the .docx plan), and (3) the committed implementation report (commit 5334893). It identifies every gap in each direction and prescribes step-by-step corrective actions, organised by priority. Prompt → Plan Gaps Plan → Implementation Gaps Fully Implemented 9 features in prompt not addressed in plan 8 plan phases partially or not implemented Migrations 0043–0050, core service, router, notifications, archive search, pipeline, dual approval (partial) NOTE: The implementation is structurally sound — migrations, service layer, and core API are all in place. The gaps are primarily in frontend workflow dialogs, permission enforcement granularity, workload balancing UI, full push-back notification chains, admin deletion governance UI, and integration trigger coverage for non-Sales apps. 2. Three-Way Gap Analysis 2.1 Features in Prompt NOT Addressed in the Plan The following requirements exist in the original prompt but were not covered — or only vaguely referenced — in the Phase 5 plan document. Gap ID Prompt Requirement Plan Coverage Implementation Status G-01 Auto-suggestion / search-while-typing for multi-user scalability in assignee selector (Section 2) Mentioned as 'dynamic search' in Phase 2.3 only at schema level MISSING — no frontend multi-select implementation G-02 Workload balancing capability in assignment system (Section 7) Not mentioned anywhere in the plan MISSING — no workload data shown in assignment UI G-03 Skill/team-based assignment (Section 7) Not mentioned anywhere in the plan MISSING — no skill/team filter on assignment G-04 Multi-level assignment logs &amp; assignment analytics (Section 7) task_assignment_logs table only — no analytics endpoint MISSING — analytics API and UI not implemented G-06 Reopened tasks pipeline stage (Section 9) PIPELINE_STAGES = 5 fixed stages; no reopen flow defined MISSING — no reopen endpoint or stage G-07 Reference-linked projects in pipeline (Section 9) archive_references table created, no pipeline linkage UI MISSING — no cross-project reference view G-08 Mention notifications: comment preview in notification (Section 8) Mention notification fires but payload not specified PARTIAL — no comment preview text in notification body G-09 Admin deletion: department manager notification with mandatory format (Section 14) Soft-delete implemented; notification format not specified PARTIAL — notification fires but no standardised format enforced 2.2 Plan Phases NOT Fully Implemented The following phases exist in the plan but were either not implemented, only partially implemented, or the implementation deviates from the plan specification. Gap ID Prompt Requirement Plan Coverage Implementation Status P-01 Phase 3 — Internal Task Workflow: push forward/backward between staff, escalation to supervisor UI (Plan p.3) Service methods exist (forward, push_back, escalate) PARTIAL — no frontend dialogs for forward/push-back/escalate in task detail P-02 Phase 5 — Manager assignment of dept tasks: only managers/leads/admins can assign; skill-based and workload-balanced (Plan p.5) Backend permission check on assign endpoint exists PARTIAL — no workload display, no skill filter; DeptInboxPage only shows assign button P-03 Phase 6 — Push-back notification chain: notify current assignee, previous assignee, previous department, dept manager, AND project creator (Plan p.6) _notify called in push_back_task() but chain not fully implemented PARTIAL — only current parties notified; chain incomplete P-04 Phase 7 — Pipeline: pending approvals, rejected phases, reopened tasks, archived projects as distinct board views (Plan p.7) 5-stage pipeline board implemented at GET /tasks/pipeline PARTIAL — no pending-approvals filter view, no reopened-tasks view P-05 Phase 11 — Forward/Backward: mandatory rejection reason UI dialog (Plan p.11) Backend validates reason field as required TEXT NOT NULL MISSING — no push-back dialog modal on frontend P-06 Phase 12 — Admin Deletion: admin must confirm with mandatory reason; system logs admin ID + timestamp + task ID + reason; dept manager notified (Plan p.12) soft_delete() and purge() backend methods exist with reason PARTIAL — no admin deletion UI with confirmation modal; no confirmation that dept manager is notified on delete P-07 Phase 13 — Audit: IP/device metadata capture (noted optional in plan) AuditLog model has ip_address column (from existing system) PARTIAL — ip_address not passed into _log_audit_event() calls in new service P-08 Phase 16 — QA, Testing &amp; Deployment Checklist (Plan p.16) py_compile and vite build pass; no test suite referenced MISSING — no unit/integration tests, no E2E checklist 2.3 Confirmed Fully Implemented Items The following items are present in the prompt, covered in the plan, and confirmed implemented in the report. No corrective action required. # Feature Evidence 1 Migrations 0043–0050 — all 8 applied, schema complete Migration Chain in report, all 8 files listed 2 task_scope field (internal/external) with validator Migration 0043 + CreateTaskRequest validator 3 External task auto-generates project via ProjectAutoGenerator auto_generator.py, create_external_task() service method 4 Department-scoped assignee fetch for internal tasks GET /tasks/staff/department endpoint 5 5-stage pipeline: backlog → in_progress → review → unit_done → archive PIPELINE_STAGES constant + GET /tasks/pipeline 6 Dual approval: assignee mark-done + creator approve-done mark_done(), approve_done(), reject_done() + router endpoints 7 @archive full-text search with GIN index Migration 0049 GIN index + GET /tasks/archive/search 8 Comment system with @mention parsing → notifications _parse_mentions(), POST /{id}/comments 9 Department routing history (task_department_routing table) Migration 0046, TaskDeptRouting model, GET /{id}/pipeline-history 10 Soft delete with reason + restore + admin purge soft_delete(), restore_task(), purge_task() methods 11 Task rejection logs (task_rejection_logs table) Migration 0045, TaskRejectionLog ORM 12 Assignment history logs (task_assignment_logs table) Migration 0044, TaskAssignmentLog ORM 13 Notification system extended: category, sender, related task/project/dept Migration 0047, notifications/service.py 14 Dept Inbox page for managers/leads (roleLevel &gt;= 3) DeptInboxPage.tsx, GET /tasks/department-inbox 15 NewTaskModal 3-step flow with ArchiveSearchPopover NewTaskModal.tsx rewrite 16 ProjectsPage: no manual creation; shows auto-generated projects with badge ProjectsPage.tsx rewrite 17 NotificationsPage with category sidebar and 5 category groups NotificationsPage.tsx rewrite 18 Sales webhook refactored through task engine webhooks/router.py _handle_sales() refactor 19 Celery daily deadline reminder task task_reminders.py + celery_app.py beat schedule 20 Full audit logging on every workflow action _log_audit_event() called in service.py throughout 21 Tenant isolation + RLS on all new tables RLS policies in migrations 0044, 0045, 0046, 0047, 0048 3. Corrective Implementation Plan The corrective actions below are grouped by functional area, ordered by priority (P0 = system-blocking, P1 = high, P2 = medium). Each step references the gap ID it resolves and provides exact file locations and acceptance criteria. MANDATORY RULE: Complete and test each step before moving to the next. Seek approval at each P0 step before proceeding. Do not modify migration files — all schema is in place. A Frontend Workflow Dialog Modals P0 — BLOCKING The backend endpoints for forward, push-back, escalate, and push-back-to-department are fully implemented. The frontend has no dialog modals to trigger them. Users cannot move tasks through the workflow. This is the highest-priority gap. A.1 Push-Back Dialog Modal [Gaps: P-01, P-05, P-03] Step Action File / Location Priority Effort A.1.1 Create PushBackModal.tsx component with: text area for mandatory rejection reason, recipient selector (previous assignee or department), submit button that calls tasksApi.pushBack(taskId, reason, toUserId) frontend/src/pages/tasks/components/PushBackModal.tsx P0 3–4 hrs A.1.2 Validate rejection reason field — minimum 10 characters, cannot be empty. Show inline error if blank. Mirror the backend TEXT NOT NULL constraint. PushBackModal.tsx — form validation P0 1 hr A.1.3 Add 'Push Back' action button to TaskDetailPage.tsx (or TaskCard menu). Show button only when user is current assignee OR manager. On click, open PushBackModal. frontend/src/pages/tasks/TaskDetailPage.tsx P0 2 hrs A.1.4 On successful push-back, invalidate task detail query and show toast: 'Task returned. Reason logged and parties notified.' TaskDetailPage.tsx — React Query invalidation P0 0.5 hr A.2 Forward-to-User Dialog Modal [Gap: P-01] Step Action File / Location Priority Effort A.2.1 Create ForwardModal.tsx with: searchable staff dropdown (reuse getDeptStaff), optional note textarea, 'Forward Task' confirm button calling tasksApi.forward(taskId, toUserId, note). frontend/src/pages/tasks/components/ForwardModal.tsx P0 2–3 hrs A.2.2 Add 'Forward to Staff' button in TaskDetailPage. Visible only for internal tasks when user is current assignee, manager, or team lead. frontend/src/pages/tasks/TaskDetailPage.tsx P0 1 hr A.2.3 On success, display confirmation: 'Task forwarded to [name]. Assignment log updated.' ForwardModal.tsx — onSuccess handler P0 0.5 hr A.3 Forward-to-Department Dialog Modal [Gap: P-01] Step Action File / Location Priority Effort A.3.1 Create ForwardDeptModal.tsx with: searchable department dropdown (fetch all departments), optional note, 'Send to Department' button calling tasksApi.forwardDept(taskId, toDeptId, note). frontend/src/pages/tasks/components/ForwardDeptModal.tsx P0 2–3 hrs A.3.2 Show 'Forward to Department' button on external task detail only. Visible to: current assignee, manager, team lead, or admin. frontend/src/pages/tasks/TaskDetailPage.tsx P0 1 hr A.3.3 On success: invalidate dept-inbox query in the receiving department AND show user a confirmation: 'Task routed to [Department]. They have been notified.' ForwardDeptModal.tsx + query invalidation P0 0.5 hr A.4 Escalation Modal [Gap: P-01] Step Action File / Location Priority Effort A.4.1 Create EscalateModal.tsx with: note textarea (required, min 20 chars), 'Escalate to Manager' confirm button calling tasksApi.escalate(taskId, note). frontend/src/pages/tasks/components/EscalateModal.tsx P1 1.5 hrs A.4.2 Show 'Escalate' action in TaskDetailPage action menu. Visible to any staff member who is the current assignee. frontend/src/pages/tasks/TaskDetailPage.tsx P1 0.5 hr A.5 Return-to-Department Dialog [Gap: P-01] Step Action File / Location Priority Effort A.5.1 Create ReturnDeptModal.tsx with: reason textarea (required), preview of 'returning to: [previous dept name]' auto-resolved from pipeline history, confirm button calling tasksApi.returnDept(taskId, reason). frontend/src/pages/tasks/components/ReturnDeptModal.tsx P0 2 hrs A.5.2 Pre-fetch GET /{id}/pipeline-history to resolve the previous department name for display in the modal. If no previous department exists, disable the return option with tooltip: 'No previous department in routing chain.' ReturnDeptModal.tsx — useQuery pipeline-history P0 1 hr B Admin Task Deletion Governance UI P0 — BLOCKING The backend soft_delete endpoint exists and requires a reason. The prompt requires a full governance UI: admin confirmation modal, mandatory reason, and verified notification to the creator's department manager. None of this is in the current frontend. B.1 Admin Deletion Confirmation Modal [Gaps: G-09, P-06] Step Action File / Location Priority Effort B.1.1 Create AdminDeleteModal.tsx with: task info summary (ID, title, department), mandatory reason textarea (min 20 chars, cannot be empty), typed confirmation ('Type CONFIRM to proceed'), 'Delete Task' button calling tasksApi.softDelete(taskId, reason). frontend/src/pages/tasks/components/AdminDeleteModal.tsx P0 3 hrs B.1.2 Add 'Delete Task' action to TaskDetailPage action menu. Show ONLY when: user has tasks.delete permission AND task.is_deleted === false. Style with red/danger color. frontend/src/pages/tasks/TaskDetailPage.tsx P0 1 hr B.1.3 On successful deletion, display toast: 'Task [TICKET-ID] deleted. Department manager has been notified.' Then navigate back to the task list. AdminDeleteModal.tsx — onSuccess P0 0.5 hr B.1.4 Add backend enforcement: in soft_delete() service method, after setting is_deleted=True, call _notify() targeting the department manager of the task creator's department. Use notification body format: 'Task [ticket] was deleted by [admin name]. Reason: [reason]'. This standardises the notification format required by the prompt. backend/app/modules/tasks/service.py — soft_delete() P0 1.5 hrs B.2 Soft-Deleted Tasks Recovery UI [Gap: P-06] Step Action File / Location Priority Effort B.2.1 Create a DeletedTasksPage.tsx that fetches GET /tasks/deleted. Show task cards with ticket, title, deleted_at, deletion_reason, deleted_by name. Include a 'Restore' button per row calling tasksApi.restore(taskId). frontend/src/pages/tasks/DeletedTasksPage.tsx P1 3 hrs B.2.2 Add route /tasks/deleted to App.tsx with lazy import. Add link in admin navigation sidebar, visible only to users with tasks.admin permission. frontend/src/App.tsx, navigation sidebar component P1 0.5 hr B.2.3 Add 'Permanent Purge' button on each deleted task card. Show confirmation dialog: 'This action is irreversible.' Call tasksApi.purge(taskId) on confirm. Visible only to super-admin role. DeletedTasksPage.tsx P1 1.5 hrs C Notification Chain — Push-Back Full Coverage P1 — HIGH The prompt requires push-back to notify: current assignee, previous assignee, previous department (all members), department manager, and project creator. The current push_back_task() method only notifies the direct sender. The chain is incomplete. C.1 Complete Push-Back Notification Chain [Gap: P-03] Step Action File / Location Priority Effort C.1.1 In push_back_task() in service.py, after logging the TaskRejectionLog, retrieve the full routing chain from task_department_routing for the task. Identify: (a) previous department, (b) previous assignee from task_assignment_logs. backend/app/modules/tasks/service.py — push_back_task() P1 2 hrs C.1.2 Call _notify_department(db, previous_dept_id, ...) with category='task_pushed_back' to notify all members of the previous department. Exclude the push-back actor. backend/app/modules/tasks/service.py — push_back_task() P1 1 hr C.1.3 Notify the previous assignee individually (may not be a department member if reassigned externally). Retrieve from task_assignment_logs ORDER BY created_at DESC OFFSET 1 LIMIT 1. backend/app/modules/tasks/service.py — push_back_task() P1 1 hr C.1.4 Notify the project creator if task.project_id is set (external task that has a linked project). Retrieve project.created_by and notify with body including the rejection reason. backend/app/modules/tasks/service.py — push_back_task() P1 0.5 hr C.1.5 Add rejection reason to all notification bodies in a standardised format: 'Task [TICKET] was pushed back by [dept/user]. Reason: [reason]' — matching the prompt example format. backend/app/modules/tasks/service.py P1 0.5 hr C.2 Mention Notifications — Comment Preview [Gap: G-08] Step Action File / Location Priority Effort C.2.1 In the POST /{id}/comments endpoint handler, when _parse_mentions() returns usernames, include the first 150 characters of the comment body in the notification body field. Format: '@[you] was mentioned: "[comment preview]..."' backend/app/modules/tasks/router.py — comment POST handler P2 1 hr C.2.2 Update the NotificationsPage mention card to render the comment preview text below the title in a grey italic style. frontend/src/pages/notifications/NotificationsPage.tsx P2 0.5 hr E Pipeline Board — Pending Approvals &amp; Reopen Views P1 — HIGH The prompt requires the pipeline to show distinct views for pending approvals, rejected phases, and reopened tasks. The current GET /tasks/pipeline returns a stage-based board but does not separate these special states. E.1 Pending Approvals Pipeline View [Gap: P-04] Step Action File / Location Priority Effort E.1.1 Add query parameter to GET /tasks/pipeline: ?view=pending_approvals. Filter: tasks where assignee_confirmed_done=True AND creator_approved_done=False. This surfaces tasks waiting for creator review. backend/app/modules/tasks/router.py — GET /tasks/pipeline P1 1.5 hrs E.1.2 In the frontend PipelinePage (or TasksPage pipeline tab), add a 'Pending Approvals' tab/filter. When active, call getPipelineBoard with view='pending_approvals'. Show task cards with 'Awaiting Your Approval' badge if the current user is the creator. frontend/src/pages/tasks/ — pipeline view component P1 2 hrs E.1.3 On each pending-approval card, show 'Approve' and 'Reject' inline buttons. Clicking Approve calls tasksApi.approveDone(taskId); clicking Reject opens a small reason dialog calling tasksApi.rejectDone(taskId, reason). frontend — pipeline card component P1 2 hrs E.2 Reopen Flow [Gap: G-06] Step Action File / Location Priority Effort E.2.1 Add POST /{task_id}/reopen endpoint. Logic: if task.pipeline_stage='archive' AND task.is_deleted=False, set pipeline_stage='in_progress', reset assignee_confirmed_done=False, creator_approved_done=False, log audit event 'task_reopened', notify current assignee. backend/app/modules/tasks/router.py + service.py P1 2 hrs E.2.2 Add tasksApi.reopen(taskId) method calling POST /{id}/reopen. frontend/src/api/tasks.api.ts P1 0.5 hr E.2.3 Show 'Reopen Task' button on archived task detail pages. Visible to: creator, dept manager, admin. On click, confirm dialog: 'This will move the task back to In Progress.' Then call reopen. frontend/src/pages/tasks/TaskDetailPage.tsx P1 1.5 hrs E.2.4 Add ?view=reopened query param to GET /tasks/pipeline, filtering tasks where pipeline_stage was previously 'archive' and is now 'in_progress' (use audit log OR add a reopened_count INTEGER column via migration). Simplest approach: add flag is_reopened BOOLEAN DEFAULT FALSE to tasks via a new migration 0051. backend — migration 0051 + pipeline endpoint P1 2 hrs F Workload Balancing &amp; Assignment Analytics P2 — MEDIUM The prompt specifically requires workload balancing capability and assignment analytics in the assignment system. These are data features built on top of the existing task_assignment_logs table. F.1 Workload Display in Assignment Dropdowns [Gaps: G-02, P-02] Step Action File / Location Priority Effort F.1.1 Update GET /tasks/staff/department response to include per-user workload data: active_task_count (non-archived, non-deleted tasks assigned to user), overdue_task_count. Add a LEFT JOIN COUNT subquery to the existing staff fetch. backend/app/modules/tasks/router.py — GET /tasks/staff/department P2 2 hrs F.1.2 Update DeptStaffMember TypeScript interface to include active_task_count: number and overdue_task_count: number. frontend/src/api/tasks.api.ts P2 0.5 hr F.1.3 In DeptInboxPage AssignModal staff selector, show workload indicator next to each staff name: '[Name] — [N] active tasks' in grey. Highlight in amber if overdue_task_count &gt; 0. This gives managers workload visibility at assignment time. frontend/src/pages/tasks/DeptInboxPage.tsx P2 1.5 hrs F.2 Assignment Analytics API [Gap: G-04] Step Action File / Location Priority Effort F.2.1 Add GET /tasks/analytics/assignments endpoint. Response: per-user totals of assignments received, assignments forwarded, tasks completed, average completion days — all from task_assignment_logs and task_completion_approvals. Filter by date range (query params: from_date, to_date) and department_id. backend/app/modules/tasks/router.py P2 3 hrs F.2.2 Add GET /tasks/analytics/departments endpoint. Response: per-department totals of tasks received, completed, pushed back, average time-in-dept. Sourced from task_department_routing. backend/app/modules/tasks/router.py P2 2.5 hrs F.2.3 Create a basic AssignmentAnalyticsPage.tsx with two tables: (a) staff table with columns Name, Assigned, Completed, Forwarded, Avg Days; (b) department table with Dept Name, Received, Completed, Pushed Back, Avg Days. Wire to the two new endpoints above. frontend/src/pages/tasks/AssignmentAnalyticsPage.tsx P2 4 hrs F.2.4 Add route /tasks/analytics to App.tsx. Add link in navigation for users with roleLevel &gt;= 3 (managers and above). frontend/src/App.tsx P2 0.5 hr G Audit — IP Address &amp; Device Metadata Capture P2 — MEDIUM G.1 Pass IP Address into Audit Events [Gap: P-07] Step Action File / Location Priority Effort G.1.1 The AuditLog model already has ip_address column (existing system). Update _log_audit_event() in service.py to accept an optional ip_address parameter. backend/app/modules/tasks/service.py — _log_audit_event() P2 0.5 hr G.1.2 In all router.py endpoints that call service methods that log audit events, extract the client IP from the FastAPI Request object: request.client.host. Pass it through to the service method as ip_address. backend/app/modules/tasks/router.py — all action endpoints P2 2 hrs G.1.3 If the platform is behind a reverse proxy (Nginx/Traefik), extract from X-Forwarded-For header first: ip = request.headers.get('X-Forwarded-For', request.client.host).split(',')[0].strip(). Add this as a utility function in core/utils.py. backend/app/core/utils.py P2 0.5 hr H Archive Reference — Cross-Project Linkage View P2 — MEDIUM H.1 Reference-Linked Projects View [Gap: G-07] Step Action File / Location Priority Effort H.1.1 Add GET /tasks/{task_id}/archive-references endpoint. Returns all ArchiveReference records for a task: referenced task/project ticket, title, pipeline_stage, link to original task. backend/app/modules/tasks/router.py P2 1.5 hrs H.1.2 In TaskDetailPage.tsx, add an 'Archive References' section below the comments panel. If archive_refs.length &gt; 0, show a list of linked archive items: ticket chip, title, 'View' link. If empty, show nothing. frontend/src/pages/tasks/TaskDetailPage.tsx P2 2 hrs I QA, Testing &amp; Deployment Checklist P1 — HIGH The plan specifies a Phase 16 QA and deployment checklist. The implementation report confirms only py_compile and vite build were run. A production system of this complexity requires structured testing. I.1 Backend Unit &amp; Integration Tests [Gap: P-08] Step Action File / Location Priority Effort I.1.1 Write pytest unit tests for TaskWorkflowService: test create_internal_task (same-dept assignee), test create_external_task (auto-project created, task_dept_routing record exists), test push_back_task (rejection log created, reason required), test dual approval flow (mark_done → approve_done → archive), test soft_delete (is_deleted=True, reason stored). backend/tests/test_task_service.py P1 6 hrs I.1.2 Write API integration tests for critical router endpoints using pytest + httpx AsyncClient: POST /tasks (internal + external), POST /{id}/assign, POST /{id}/push-back, POST /{id}/mark-done, POST /{id}/approve-done, DELETE /{id} (soft-delete). backend/tests/test_task_router.py P1 5 hrs I.1.3 Write migration integrity test: run all 8 migrations (0043–0050) on a clean test DB, then downgrade all, verify no data loss on re-upgrade. backend/tests/test_migrations.py P1 2 hrs I.2 Frontend Component Tests [Gap: P-08] Step Action File / Location Priority Effort I.2.1 Write Vitest + React Testing Library tests for NewTaskModal: (a) scope selection shows correct form, (b) internal form shows dept-scoped staff, (c) external form auto-generates project on submit (mock API), (d) ArchiveSearchPopover returns and adds results. frontend/src/__tests__/NewTaskModal.test.tsx P1 4 hrs I.2.2 Write tests for DeptInboxPage: (a) renders pending task cards, (b) assign button opens modal with staff list, (c) access-denied screen for non-managers, (d) auto-refresh fires after 60 seconds (use fake timers). frontend/src/__tests__/DeptInboxPage.test.tsx P1 3 hrs I.2.3 Write tests for all new dialog modals (PushBackModal, ForwardModal, AdminDeleteModal): render, validation errors on empty submit, success callbacks. frontend/src/__tests__/workflow-modals.test.tsx P1 3 hrs I.3 Deployment Pre-Flight Checklist Step Action File / Location Priority Effort I.3.1 Confirm Alembic migration head is at 0050 (or 0051 if reopen flag is added). Run: alembic current. Verify on staging DB before production. Staging environment — alembic CLI P0 0.5 hr I.3.2 Confirm all 8 new migration files are committed: ls backend/alembic/versions/004[3-9]* backend/alembic/versions/0050* CI/CD pipeline — pre-deploy check P0 0.25 hr I.3.3 Run database RLS policy verification: connect as a tenant-scoped DB role and confirm task_assignment_logs, task_rejection_logs, task_department_routing only return records for that tenant. Staging DB — manual SQL verification P0 1 hr I.3.4 Celery Beat: confirm send-task-deadline-reminders task appears in beat schedule and fires on a test run: celery -A app.core.celery_app beat --loglevel=info Staging Celery worker P1 0.5 hr I.3.5 End-to-end smoke test: create internal task → assign → push back → re-assign → mark done → approve done → verify archived. Then create external task → verify project auto-created → dept inbox entry appears → assign from inbox → forward dept → archive. Staging environment — manual QA P0 2 hrs 4. Implementation Priority Matrix The table below summarises all corrective actions by group, priority, estimated effort, and recommended sequencing. Group Title Priority Est. Effort Sequence A Frontend Workflow Dialog Modals P0 ~15 hrs Do first — users blocked B Admin Deletion Governance UI P0 ~7 hrs Do alongside A I.3 Deployment Preflight Checklist P0 ~4 hrs Before any production deploy C Push-Back Notification Chain P1 ~6 hrs After A is complete D HR/Field/Inventory/Integration Webhooks P1 ~13 hrs After C E Pipeline Pending Approvals &amp; Reopen P1 ~10 hrs After D I.1-I.2 Unit &amp; Integration Tests P1 ~23 hrs Parallel with D and E F Workload Balancing &amp; Analytics P2 ~12 hrs After E G Audit IP Address Capture P2 ~3 hrs Low effort — do early in P2 H Archive Cross-Reference View P2 ~3.5 hrs Last — nice to have Total estimated corrective effort: ~97 hours of development time. P0 items alone: ~26 hours. Recommend a focused 3-day sprint for P0 items before any Phase 5 feature is used in production. 5. Acceptance Criteria — Definition of Done Each corrective group is considered complete only when ALL of the following criteria are met: # Group Acceptance Criteria A Workflow Dialogs All 5 dialog modals (PushBack, Forward, ForwardDept, Escalate, ReturnDept) render, validate, call correct API, and show success/error feedback. Buttons appear/hide correctly based on user role and task scope. B Admin Deletion UI AdminDeleteModal requires typed CONFIRM and non-empty reason. Deletion triggers department manager notification. DeletedTasksPage shows deleted tasks with restore and purge options. C Notification Chain After a push-back: current assignee, previous assignee, all members of previous department, and project creator all receive in-app notifications with rejection reason in body. D Webhook Handlers All 5 source apps (sales, hr, field, inventory, integration) create tasks + auto-generate projects via task engine. source_app field is set correctly on both task and project records. E Pipeline Views Pending Approvals tab shows tasks with assignee_confirmed_done=True AND creator_approved_done=False. Reopen flow moves archived task back to in_progress and resets approval flags. F Workload Analytics Assignment dropdown shows active task count per staff member. Analytics pages return correct per-user and per-department totals filterable by date range. G Audit IP Every workflow action in audit_logs has ip_address populated (not null) for requests made through the API. H Archive References Task detail page shows linked archive references when present. Archive search popover in NewTaskModal returns and saves references correctly. I Tests &amp; QA pytest suite passes with &gt;80% service layer coverage. All 5 smoke test scenarios (internal task, external task, push-back, dual approval, admin delete) pass on staging. 6. Sign-Off Role Name Approval Date Platform Lead Backend Lead Frontend Lead QA Lead No corrective phase should be merged to main without sign-off from the Platform Lead. All P0 items require sign-off before any production deployment of Phase 5 features. 
